use bevy::prelude::*;

// ================================
// ========= DebugPlugin ==========
// ================================
pub struct DebugPlugin;

impl Plugin for DebugPlugin {
    fn build(&self, app: &mut App) {
    }
}
